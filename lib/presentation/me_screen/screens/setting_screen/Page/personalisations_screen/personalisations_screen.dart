import 'package:flutter/material.dart';

class PersonalisationsScreen extends StatelessWidget {
  const PersonalisationsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [

        ],
      ),
    );
  }
}
